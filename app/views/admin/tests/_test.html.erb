<tbody>
  <tr>
    <td><%= link_to test.title, admin_test_path(test), method: :get %></td>
    <td><%= link_to t(:update, scope: :base), edit_admin_test_path(test) %></td>
    <td><%= link_to t(:delete, scope: :base), admin_test_path(test), method: :delete, data: { confirm: t(:delete_confirm, scope: :base) } %></td>
    <td><%= button_to t('.start'), start_test_path(test) %></td>
    <td><%= test.questions.count %></td>
  </tr>
</tbody>
